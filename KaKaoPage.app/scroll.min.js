function addEventListenerForScroll(){let isScrollCheckRunning=false;let stationaryFrames=0;let lastScrollX=window.scrollX;let lastScrollY=window.scrollY;const FRAMES_TO_WAIT=5;let scrollStartTime=0;const MAX_SCROLL_DURATION=1e3;window.addEventListener("scroll",function(){if(isScrollCheckRunning)return;const currentX=window.scrollX;const currentY=window.scrollY;if(currentX===lastScrollX&&currentY===lastScrollY){return}isScrollCheckRunning=true;stationaryFrames=0;scrollStartTime=Date.now();checkScrollStop()});function checkScrollStop(){window.requestAnimationFrame(function(){const currentX=window.scrollX;const currentY=window.scrollY;const now=Date.now();if(now-scrollStartTime>MAX_SCROLL_DURATION){didScrollToPage(currentX,currentY);return}if(currentX===lastScrollX&&currentY===lastScrollY){stationaryFrames++}else{stationaryFrames=0;lastScrollX=currentX;lastScrollY=currentY}if(stationaryFrames>=FRAMES_TO_WAIT){didScrollToPage(lastScrollX,lastScrollY)}else{checkScrollStop()}})}function didScrollToPage(finalX,finalY,reason){isScrollCheckRunning=false;stationaryFrames=0;lastScrollX=finalX;lastScrollY=finalY;if(window.webkit&&window.webkit.messageHandlers.didScrollToPage){window.webkit.messageHandlers.didScrollToPage.postMessage(getCurrentPageIndex())}}}function getCurrentPageIndex(){let index=0;if(isScrollMode()){const clientHeight=getClientHeight();if(clientHeight>0){const contentScrollHeight=getContentScrollHeight();const scrollTop=document.scrollingElement.scrollTop;const remainder=contentScrollHeight%clientHeight;const lastPageHeight=remainder===0?clientHeight:remainder;if(scrollTop+clientHeight>=contentScrollHeight-lastPageHeight/2){index=Math.ceil(contentScrollHeight/clientHeight)-1}else{index=Math.round(scrollTop/clientHeight)}}}else{const clientWidth=getClientWidth();if(clientWidth>0){index=Math.floor(document.scrollingElement.scrollLeft/clientWidth)}}return Math.max(0,index)}function scrollPage(index){if(isScrollMode()){scrollRowPage(index)}else{scrollColumnPage(index)}}function scrollColumnPage(index){const clientWidth=getClientWidth();const offsetX=index*clientWidth;document.scrollingElement.scrollLeft=offsetX}function scrollRowPage(index){const clientHeight=getClientHeight();const offsetY=index*clientHeight;if(index==0){document.scrollingElement.scrollTop=offsetY}else{var maxY=Math.max(0,getContentScrollHeight()-clientHeight);document.scrollingElement.scrollTop=Math.min(offsetY,maxY)}}function scrollLeftToRect(rect,animated,completionHandler){const viewportRect=getViewportRect();if(isIntersecting(rect,viewportRect)){if(completionHandler){completionHandler()}return}const startX=document.documentElement.scrollLeft;const absoluteLeft=rect.left+startX;const targetScreenIndex=Math.floor(absoluteLeft/viewportRect.width);const targetScrollPosition=targetScreenIndex*viewportRect.width;if(document.hidden||!animated){window.scrollTo(targetScrollPosition,0);if(completionHandler){completionHandler()}}else{const distance=targetScrollPosition-startX;const duration=200;let startTime=null;function animateScroll(currentTime){if(startTime===null)startTime=currentTime;const elapsedTime=currentTime-startTime;const progress=Math.min(elapsedTime/duration,1);window.scrollTo(startX+distance*progress,0);if(elapsedTime<duration){requestAnimationFrame(animateScroll)}else if(completionHandler){completionHandler()}}requestAnimationFrame(animateScroll)}}function scrollTopToRect(rect,animated,completionHandler){const startY=document.documentElement.scrollTop;const absoluteTop=rect.top+startY;const offset=getClientHeight()*.3;const targetScrollPosition=absoluteTop-offset;if(document.hidden||!animated){window.scrollTo(0,targetScrollPosition);if(completionHandler){completionHandler()}}else{const distance=targetScrollPosition-startY;const duration=200;let startTime=null;function animateScroll(currentTime){if(startTime===null)startTime=currentTime;const elapsedTime=currentTime-startTime;const progress=Math.min(elapsedTime/duration,1);window.scrollTo(0,startY+distance*progress);if(elapsedTime<duration){requestAnimationFrame(animateScroll)}else if(completionHandler){completionHandler()}}requestAnimationFrame(animateScroll)}}